
server {
    listen 80;
    server_name nubium-sandbox.test *.nubium-sandbox.test;
    root ../:/var/www/html;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php$is_args$args;
    }
}